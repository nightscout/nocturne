<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Interfaces, settings records and the direction/format helpers only; no Windows API is
         referenced, so this stays platform-neutral and its tests run on the Linux CI host. -->
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\Core\Nocturne.Core.Models\Nocturne.Core.Models.csproj" />
  </ItemGroup>
</Project>
