// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { UpsertTimezoneEntryRequestSchema, RecorrectRequestSchema } from '$lib/api/generated/schemas';
import { type UpsertTimezoneEntryRequest, type RecorrectRequest } from '$api';

/** Get the tenant's timezone timeline, ordered by effective date. */
export const getTimeline = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.timezoneTimeline.getTimeline();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in timezoneTimeline.getTimeline:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get timeline');
  }
});

/** Create or update a timeline entry. EffectiveFrom is a local wall-clock date/time (the
moment, in local terms, that the zone took effect). A trip is two entries (out + return); a
move is a single entry; the origin entry covers all earlier history. */
export const upsert = command(UpsertTimezoneEntryRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.timezoneTimeline.upsert(request as UpsertTimezoneEntryRequest);
    await Promise.all([
      getTimeline(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in timezoneTimeline.upsert:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to upsert');
  }
});

/** Delete a timeline entry. */
export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.timezoneTimeline.delete(id);
    await Promise.all([
      getTimeline(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in timezoneTimeline.delete:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to remove');
  }
});

/** Re-correct already-imported data after a timeline change by re-pulling the affected window from
Glooko. Records re-flow the normal publish path and upsert in place on their stable
SyncIdentifier, so timestamps move without duplicating. Intended to be called after the user
confirms the affected window. Runs synchronously; the window is bounded. */
export const recorrect = command(RecorrectRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.timezoneTimeline.recorrect(request as RecorrectRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in timezoneTimeline.recorrect:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to recorrect');
  }
});
