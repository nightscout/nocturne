// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { AuditConfigDtoSchema } from '$lib/api/generated/schemas';
import { type AuditConfigDto } from '$api';

/** Query mutation audit log entries for the current tenant. */
export const getMutationAuditLog = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), subjectId: z.string().optional(), entityType: z.string().optional(), action: z.string().optional(), entityId: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.audit.getMutationAuditLog(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.subjectId, params?.entityType, params?.action, params?.entityId);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in audit.getMutationAuditLog:', err);
    throw error(500, 'Failed to get mutation audit log');
  }
});

/** Query read access audit log entries for the current tenant. */
export const getReadAccessAuditLog = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), subjectId: z.string().optional(), entityType: z.string().optional(), endpoint: z.string().optional(), statusCode: z.number().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.audit.getReadAccessAuditLog(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.subjectId, params?.entityType, params?.endpoint, params?.statusCode);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in audit.getReadAccessAuditLog:', err);
    throw error(500, 'Failed to get read access audit log');
  }
});

/** Get the audit configuration for the current tenant. */
export const getAuditConfig = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.audit.getAuditConfig();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in audit.getAuditConfig:', err);
    throw error(500, 'Failed to get audit config');
  }
});

/** Create or update the audit configuration for the current tenant. */
export const updateAuditConfig = command(AuditConfigDtoSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.audit.updateAuditConfig(request as AuditConfigDto);
    await Promise.all([
      getAuditConfig(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in audit.updateAuditConfig:', err);
    throw error(500, 'Failed to update audit config');
  }
});
