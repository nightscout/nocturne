// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { ClusterConfidence } from '$api';

/** Analyze a UTC window for sensor-integrity clusters and cluster-linked hypo events. */
export const analyze = query(z.object({ startDate: z.coerce.date().optional(), endDate: z.coerce.date().optional(), source: z.string().optional(), bySource: z.boolean().optional(), minConfidence: z.enum(ClusterConfidence).optional(), requireInsulin: z.boolean().optional(), hypoThresholdMgdl: z.number().optional(), windowHours: z.number().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.sensorIntegrity.analyze(params?.startDate, params?.endDate, params?.source, params?.bySource, params?.minConfidence, params?.requireInsulin, params?.hypoThresholdMgdl, params?.windowHours);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in sensorIntegrity.analyze:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to analyze');
  }
});
