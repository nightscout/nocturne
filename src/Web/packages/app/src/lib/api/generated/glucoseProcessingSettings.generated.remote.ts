// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { SetGlucoseProcessingPreferenceRequestSchema, SetGlucoseProcessingSourceDefaultsRequestSchema } from '$lib/api/generated/schemas';
import { type SetGlucoseProcessingPreferenceRequest, type SetGlucoseProcessingSourceDefaultsRequest } from '$api';

export const getPreference = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.glucoseProcessingSettings.getPreference();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in glucoseProcessingSettings.getPreference:', err);
    throw error(500, 'Failed to get preference');
  }
});

export const setPreference = command(SetGlucoseProcessingPreferenceRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.glucoseProcessingSettings.setPreference(request as SetGlucoseProcessingPreferenceRequest);
    await Promise.all([
      getPreference(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in glucoseProcessingSettings.setPreference:', err);
    throw error(500, 'Failed to set preference');
  }
});

export const getSourceDefaults = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.glucoseProcessingSettings.getSourceDefaults();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in glucoseProcessingSettings.getSourceDefaults:', err);
    throw error(500, 'Failed to get source defaults');
  }
});

export const setSourceDefaults = command(SetGlucoseProcessingSourceDefaultsRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.glucoseProcessingSettings.setSourceDefaults(request as SetGlucoseProcessingSourceDefaultsRequest);
    await Promise.all([
      getSourceDefaults(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in glucoseProcessingSettings.setSourceDefaults:', err);
    throw error(500, 'Failed to set source defaults');
  }
});
