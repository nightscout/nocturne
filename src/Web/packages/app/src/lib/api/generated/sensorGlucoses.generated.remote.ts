// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command, form } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { UpsertSensorGlucoseRequestSchema } from '$lib/api/generated/schemas';
import { type UpsertSensorGlucoseRequest } from '$api';

export const getAll = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), device: z.string().optional(), source: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.sensorGlucose.getAll(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.device, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in sensorGlucose.getAll:', err);
    throw error(500, 'Failed to get all');
  }
});

export const create = form(UpsertSensorGlucoseRequestSchema as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.sensorGlucose.create(request as UpsertSensorGlucoseRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in sensorGlucose.create:', err);
    throw error(500, 'Failed to create');
  }
});

/** Retrieves a single record by its unique identifier. */
export const getById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.sensorGlucose.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in sensorGlucose.getById:', err);
    throw error(500, 'Failed to get by id');
  }
});

export const update = form(z.object({ id: z.string(), request: UpsertSensorGlucoseRequestSchema }) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.sensorGlucose.update(id, request as UpsertSensorGlucoseRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in sensorGlucose.update:', err);
    throw error(500, 'Failed to update');
  }
});

/** Deletes a record by ID. */
export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.sensorGlucose.delete(id);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in sensorGlucose.delete:', err);
    throw error(500, 'Failed to remove');
  }
});
