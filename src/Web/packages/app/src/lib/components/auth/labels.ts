/**
 * Labels for the sign-in controls, shared so the help page can quote the
 * buttons a locked-out person is actually looking at. Change them here.
 */
export const signInMethodLabels = {
  passkey: "Sign in with passkey",
  username: "Sign in with username",
  authenticator: "Use authenticator app",
  recoveryCode: "Use a recovery code",
} as const;
